apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: sonarr
  namespace: flux-system
spec:
  targetNamespace: media
  releaseName: sonarr
  interval: 30m
  chart:
    spec:
      chart: app-template
      version: "5.0.1"
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  values:
    controllers:
      main:
        pod:
          nodeSelector:
            kubernetes.io/hostname: g3
        containers:
          main:
            image:
              repository: lscr.io/linuxserver/sonarr
              tag: latest
            env:
              PUID: "977"
              PGID: "988"
              TZ: "Australia/Sydney"
            resources:
              requests: { cpu: 500m, memory: 512Mi }
              limits: { cpu: 1500m, memory: 2Gi }

    service:
      main:
        ports:
          main:
            port: 8989

    ingress:
      main:
        enabled: true
        className: nginx
        hosts:
          - host: sonarr.home.lab
            paths:
              - path: /
                pathType: Prefix
                service:
                  identifier: main
                  port: main

    persistence:
      config:
        enabled: true
        type: hostPath
        hostPath: /var/lib/media-config/sonarr
        globalMounts:
          - path: /config
      media:
        enabled: true
        existingClaim: media-data
        globalMounts:
          - path: /data
